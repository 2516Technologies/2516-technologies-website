from openai import OpenAI

client = OpenAI()

prompt = "Say hello!"

response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": prompt}]
)

print(response.choices[0].message.content)
T

from openai import OpenAI

client = OpenAI()

prompt = "Say hello!"

response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": prompt}]
)

print(response.choices[0].message.content)
python3 first_call.py
cat > first_call.py << 'EOF'
from openai import OpenAI

client = OpenAI()

prompt = "Say hello!"

response = client.chat.completions.create(
    model="gpt-4o-mini",
    messages=[{"role": "user", "content": prompt}]
)

print(response.choices[0].message.content)
EOF

